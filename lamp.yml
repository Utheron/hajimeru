version: '3'
services:

  php:
    container_name: ${CONTAINER_NAME}_HLA-php
    build:
      context: ./conf
      dockerfile: Dockerfile-lamp
    ports:
      - ${SERVER_PORT}:80
    volumes:
      - ./conf/httpd.conf:/etc/apache2/sites-available/000-default.conf
      - ./conf/php.ini:/usr/local/etc/php/php.ini
      - ./www:/var/www/html
    working_dir: /var/www/html
    restart: unless-stopped

  mysql:
    container_name: ${CONTAINER_NAME}_HLA-sql
    image: mysql:5.7
    ports:
      - ${DB_PORT}:3306
    volumes:
      - ./mysql:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PWD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    restart: unless-stopped

  phpmyadmin:
    container_name: ${CONTAINER_NAME}_HLA-pma
    image: phpmyadmin/phpmyadmin:4.7
    ports:
      - ${PMA_PORT}:80
    environment:
      PMA_HOST: ${DB_HOST}
    restart: unless-stopped

  maildev:
    container_name: ${CONTAINER_NAME}_HLA-mail
    image: djfarrelly/maildev
    ports:
      - ${MAILDEV_PORT}:80
    restart: unless-stopped